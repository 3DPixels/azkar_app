import 'package:flutter/material.dart';

class DuaaPage extends StatelessWidget {
  const DuaaPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('I am feeling'),
      ),
    );
  }
}
